apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service-node
  labels:
    app: customer-service-node
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-service-node
  template:
    metadata:
      labels:
        app: customer-service-node
    spec:
      containers:
        - name: customer-service
          image: ${REGISTRY}/${IMAGE_NAME}:${IMAGE_TAG}
          ports:
            - containerPort: 3004
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3004"
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: customer-db
                  key: host
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: customer-db
                  key: port
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: customer-db
                  key: user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: customer-db
                  key: password
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: customer-db
                  key: name
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: customer-rabbitmq
                  key: url
            - name: CORS_ORIGIN
              value: "https://tudominio.com"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 10
